"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import DashboardShell from "@/components/DashboardShell";
import { INVESTMENT_TIERS } from "@/lib/investment-tiers";
export default function InvestPage(){const router=useRouter();const[selected,setSelected]=useState<(typeof INVESTMENT_TIERS)[number]|null>(null);return <DashboardShell><h1 className="font-serif text-[26px] mb-2">Invest</h1><p className="text-paper-dim mb-8">Choose an investment option, then select whether to invest with available cash or crypto holdings.</p><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{INVESTMENT_TIERS.map(t=><button key={t.id} onClick={()=>setSelected(t)} className="text-left bg-slate border border-line hover:border-gold p-6"><div className="flex justify-between"><span className="font-serif text-[20px]">{t.name}</span><span className="font-mono text-gold text-[20px]">{t.apr}%</span></div><div className="font-mono text-paper-dim mt-4">Investment amount: ${t.min.toLocaleString()}</div><p className="text-paper-dim text-[13.5px] mt-3">{t.description}</p><span className="block mt-6 text-gold text-[13px]">Invest now →</span></button>)}</div>{selected&&<div className="fixed inset-0 z-[70] bg-ink/80 p-4 flex items-center justify-center"><div className="w-full max-w-[460px] bg-slate border border-gold p-6"><button onClick={()=>setSelected(null)} className="float-right text-paper-dim">×</button><p className="font-mono text-gold text-[12px]">{selected.name.toUpperCase()} · ${selected.min.toLocaleString()}</p><h2 className="font-serif text-[25px] mt-3">How would you like to invest?</h2><p className="text-paper-dim text-[14px] mt-2 mb-6">Choose the balance you want to use for this investment.</p><div className="grid grid-cols-1 sm:grid-cols-2 gap-3"><button onClick={()=>router.push(`/dashboard/invest/cash?tier=${selected.id}`)} className="border border-line hover:border-gold p-4 text-left"><b>Available cash</b><span className="block text-paper-dim text-[13px] mt-1">Invest from your cash balance</span></button><button onClick={()=>router.push(`/dashboard/invest/crypto?tier=${selected.id}`)} className="border border-line hover:border-gold p-4 text-left"><b>Crypto holdings</b><span className="block text-paper-dim text-[13px] mt-1">Choose a crypto asset</span></button></div></div></div>}</DashboardShell>}
